fun:_->_
fun () =
    print(1)

fun: float, float, float -> float
fun (x, y, z) =
    return x

fun: bool, int -> _
fun (x, y) =
    print(x) 

fun: string, set<string>, matrix<int>, tuple<tuple<string,int>> -> range
fun (x=1, y=2, z=4) =
    return x + 2

fun: float, int -> string
fun (x, z=5) =
    return 'hello'

fun: float, string, int -> string
fun (x, z='yes', z=5) =
    return 'hello'

fun: (int -> int), int -> int
fun (x, y) =
    return x(y)

fun: (int -> _), int -> int
fun (x, y) =
    return x(y)

fun: (_ -> _), int -> int
fun (x, y) =
    return x(y)

fun: (_ -> _), (int -> string), (int -> int) -> _
fun (x, y, z) =
    print('Hello, World!')
    print('testing that I can use " within a quote')

fun: (int, string, (int, string -> int) -> string), int -> int
fun (x, y, z) =
    return z(y)