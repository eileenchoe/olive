fun: ->
fun () =
    return

fun: float, float, float -> float
fun (x, y, z) =
    return x

fun (x = 5) =
    return x

fun: int, int, int -> int
fun (x=1, y=2, z=4) =
    return x + 2

fun: float, int -> string
fun (x, z=5) =
    return 'hello'

lambda y: y

lambda z: z + 1

lambda x, y, z: z + y + x